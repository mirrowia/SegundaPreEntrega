<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Carrito</title>

  <!-- Agregar los archivos CSS de Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
</head>

<body>
  <div class="container">
    <h1 class="mt-5">Bienvenido, {{user}}</h1>

    <h2 class="mt-4">Tu Carrito de Compras:</h2>
    <ul class="list-group mt-3">
      {{#each products}}
      <li class="list-group-item">
        <div class="row align-items-center">
          <div class="col-md-3">
            <img src="{{this._doc.image_url}}" alt="{{this._doc.name}}" class="img-fluid" width="100">
          </div>
          <div class="col-md-6">
            <h3>{{this._doc.name}}</h3>
            <p>Cantidad: {{this._doc.quantity}}</p>
          </div>
          <div class="col-md-3 text-end">
            {{!-- <p>Total: ${{multiply this.quantity this.price}}</p> --}}
          </div>
        </div>
      </li>
      {{/each}}
    </ul>
  </div>

</body>

</html>